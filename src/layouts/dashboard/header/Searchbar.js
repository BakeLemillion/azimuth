import { useState } from 'react';
// @mui
import { styled } from '@mui/material/styles';
import { Typography } from '@mui/material';
// utils

export default function Searchbar() {

  return (
    <div>
      <Typography sx={{fontWeight: "750"}} color={'black'}>ТУТ БУДЕТ ПУТЬ</Typography>
    </div>
    
  );
}
